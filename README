Build instructions
------------------

To build the system run

./all.sh

See the README of the p2peditor on instructions how to start the demo software.

samples/p2p_server and samples/p2p_client do not work currently.

If you want to experiment with the Google AppEngine support run

./gae.sh

to setup symlinks.
Then continue exploring the samples p2p_server and p2p_client